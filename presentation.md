# 基于大语言模型内部状态的幻觉检测

## 使用注意力头级探针与投票集成

---

### 作者

Tingyu Lu & Shengwen Liu  
计算机学院

---

# 研究背景与动机

---

## 大语言模型的幻觉问题

- LLM 会生成**看似流畅但包含事实错误的文本**——即"幻觉"（Hallucination）
- 在医疗问答、法律咨询、知识检索等高风险场景中，幻觉可能造成严重后果
- 例如：模型自信地声称"爱因斯坦发明了电话"，但实际是贝尔发明的

---

## 现有检测方法

| 类型 | 方法 | 优点 | 缺点 |
|------|------|------|------|
| **外部方法** | 检索外部知识库验证 | 准确度较高 | 额外延迟、基础设施成本 |
| **内部方法** | 利用模型自身内部状态 | 无需外部资源、实时检测 | 需要理解模型内部表示 |

**本研究聚焦于内部方法**，探索如何利用 LLM 的隐藏状态和注意力头信息进行幻觉检测。

---

## 研究目标

1. 复现并比较两种基线方法：**PPL（困惑度）** 与 **SAPLMA（隐藏状态探针）**
2. 分析**层深度**和 **Token 位置**对检测性能的影响
3. 提出**注意力头级探针 + 投票集成**的新方法，用极少数注意力头超越完整隐藏状态的检测效果

# 阶段一：PPL 与 SAPLMA 基线对比

---

## 方法 1：PPL（困惑度检测）

**原理**：语言模型对真实陈述的困惑度应低于虚假陈述——模型在虚假内容上更"意外"。

**计算流程**：

1. 对待检测文本进行 tokenize
2. 前向传播获取每个位置的 logits
3. 计算每个 token 的负对数似然（平均 NLL）：$\text{NLL} = -\frac{1}{T}\sum_{t=1}^{T} \log p(x_t | x_{<t})$
4. NLL 越高 → 模型认为该陈述越"意外" → 越可能是幻觉
5. 在训练集上通过 ROC 曲线寻找最优 NLL 阈值（最大化 Accuracy）

**特点**：**无参数、无需训练**，仅依赖模型输出概率分布。本质上是一个线性阈值分类器。

---

## 方法 2：SAPLMA（隐藏状态探针）

**原理**：LLM 中间层的隐藏状态编码了比输出概率更丰富、更抽象的语义表征，包括模型对陈述真实性的"内部判断"。

**特征提取流程**：

1. 输入陈述文本，执行完整前向传播（`output_hidden_states=True`）
2. 从中间层的隐藏状态中取**最后一个真实 token** 对应位置的向量：$h \in \mathbb{R}^{1536}$
3. 真实 token 位置通过 `attention_mask.sum(dim=1) - 1` 确定，排除 padding 干扰
4. `hidden_states[0]` = 嵌入层输出，`hidden_states[1]`..`[28]` = 第 1 到第 28 层 Transformer 输出

**测试层选择**：从第 14 层（中层）开始，每 2 层取一个：`[14, 16, 18, 20, 22, 24, 26, 28]`，覆盖中层到最后一层。

---

## 阶段一 · 实验设置

### 模型：Qwen2-1.5B

| 参数 | 值 |
|------|-----|
| Transformer 层数 | 28 |
| 每层注意力头数 | 12 |
| 注意力头维度 | 128 |
| 隐藏状态维度 | 1536 |
| 注意力头总数 | 28 × 12 = **336** |
| 精度 | bfloat16 |

### 数据集

6 个主题的 **True/False 数据集** —— Cities、Inventions、Elements、Animals、Companies、Facts。每个主题包含**成对的正误陈述**（如 "The Eiffel Tower is located in Paris" = True，"The Eiffel Tower is located in London" = False），CSV 格式含 `statement` 和 `label`（0/1）两列。

### 探针架构与训练配置

```
输入 1536 维 → Dense(256, ReLU) → Dense(128, ReLU) → Dense(64, ReLU) → Dense(1, Sigmoid) → P(hallucination)
```

| 超参数 | 值 |
|--------|-----|
| 优化器 | Adam |
| 损失函数 | Binary Cross-Entropy |
| 训练 Batch Size | 32 |
| 训练 Epoch | 5 |
| 随机重启次数 | 3（取最佳） |
| 验证策略 | 留一法交叉验证（5 个主题训练，1 个主题测试，轮换 6 次） |
| 阈值选取 | 在训练集 ROC 曲线上最大化 Accuracy 的阈值 |
| 特征提取 batch size | 8（逐 batch 前向传播，缓存嵌入至内存和磁盘 CSV） |

---

## 阶段一结果：PPL vs SAPLMA（按主题）

| Dataset | PPL Accuracy | PPL AUC | SAPLMA-L16 AUC |
|---------|-------------|---------|----------------|
| Cities | 0.703 | 0.740 | 0.909 |
| Inventions | 0.603 | 0.638 | 0.864 |
| Elements | 0.624 | 0.662 | 0.710 |
| Animals | 0.624 | 0.658 | 0.787 |
| Companies | 0.626 | 0.670 | 0.924 |
| Facts | 0.722 | 0.775 | 0.861 |
| **平均** | **0.650** | **0.690** | **0.843** |

> SAPLMA 在**每个主题**上均显著优于 PPL，平均 AUC 高出 0.153

---

## 阶段一结果：SAPLMA 各层性能（留一法交叉验证平均）

| Layer | Accuracy | AUC |
|-------|----------|-----|
| 14 | 0.732 | 0.834 |
| **16** | **0.751** | **0.843** |
| 18 | 0.735 | 0.832 |
| 20 | 0.726 | 0.840 |
| 22 | 0.693 | 0.816 |
| 24 | 0.689 | 0.802 |
| 26 | 0.666 | 0.778 |
| 28 | 0.659 | 0.756 |

**关键发现**：性能随层深度呈现 **"倒 U 型"曲线**，Layer 16（57% 深度）达到最优，较此前 Layer 18 的峰值更靠前；最后一层性能最差。

**解释**：中上层编码最抽象的任务相关表征，而最后几层开始将表征转化为面向下一 token 预测的形式，损失了部分判别信息。加密层测试（间隔 2 层）揭示了更精细的峰值位置。

---

# 阶段二：Token 位置分析

---

## 研究问题与方法

SAPLMA 使用**最后一个 token** 的隐藏状态——但不同位置的 token 是否承载不同质量的信息？在自回归 Transformer 中，前面的 token 只能看到有限的上下文，最后一个 token 才能聚合完整句子的语义。

**测试的四个位置**：

| 位置名称 | 索引 | 提取方式 |
|----------|------|----------|
| First | position 0 | 绝对位置 `0`，取 `hidden_states[16][batch, 0, :]` |
| Second | position 1 | 绝对位置 `1`，取 `hidden_states[16][batch, 1, :]` |
| Second-to-Last | position -2 | `seq_length - 2 + 1`，clamp 至有效范围 |
| **Last** | **position -1** | `attention_mask.sum(dim=1) - 1`（最后一个非 pad token） |

**核心假设**：位置越靠后，聚合的上下文信息越多，探针判别力越强。

---

## 阶段二 · 实验设置

- **模型与数据集**：与阶段一完全相同（Qwen2-1.5B，6 主题 True/False 数据集）
- **层选择**：固定在 **Layer 16**（阶段一确定的最优层）
- **探针与训练**：与阶段一完全相同的架构和超参数——三层前馈网络（256→128→64→1），5 epoch × 3 次随机重启，6 主题留一法交叉验证，特征提取 batch size = 8

---

## 阶段二结果：Token 位置分析（Layer 16，留一法平均）

| Position | Avg Accuracy | Avg AUC |
|----------|-------------|---------|
| First（位置 0） | 0.504 | 0.496 |
| Second（位置 1） | 0.500 | 0.496 |
| Second-to-Last（位置 -2） | 0.621 | 0.690 |
| **Last（位置 -1）** | **0.751** | **0.844** |

**关键发现**：

- 第一个和第二个 token 的 AUC 接近 0.50——**几乎随机**，说明模型在处理开始处尚未建立与陈述真值相关的表征
- 倒数第二个 token（AUC 0.690）已有一定判别力，说明**语义信息随 token 位置逐步累积**
- 最后一个 token（AUC 0.844）远超其他位置——在自回归 Transformer 中，最后一个 token "看过"完整句子，聚合了最丰富的语义表征

---

# 阶段三：注意力头级探针与投票集成

---

## 核心思路

SAPLMA 使用**完整 1536 维隐藏状态**作为特征——但 Transformer 的隐藏状态是由 28 层 × 12 头 = 336 个注意力头各自贡献的**影响向量**累加而成。不同注意力头可能编码不同类型的信息：有些负责句法结构、有些负责语义内容、有些可能与事实一致性相关。

**核心假设**：对幻觉检测最关键的信息可能集中在少数几个注意力头中。若能找到这些头，用它们的信号即可超越完整隐藏状态。

```
336 个注意力头 → 逐头训练 336 个二分类器 → 验证集 AUC 筛选 Top-5 → 特征融合与投票集成
```

---

## 步骤 1：TriviaQA 数据生成与标注

### 为什么换数据集？

True/False 数据集的陈述格式过于受限（单一陈述句），无法真实反映 LLM 在开放域问答中的幻觉行为。阶段三改用 **TriviaQA**（开放域百科问答数据集），让模型自由生成答案后再进行检测，更贴近实际应用场景。

### 数据生成流程

1. 从 TriviaQA 随机采样 1000 个问答对
2. 使用 Qwen2-1.5B 以 **greedy decoding**（`do_sample=False`，`max_new_tokens=50`）生成答案
3. 保存**生成的 token IDs**（而非仅解码后的文本），避免后续 BPE 往返误差
4. **自动标注**：将生成答案与 TriviaQA 提供的标准答案别名进行子串模糊匹配——若标准答案出现在生成文本中，则标注为"正确"（label=1），否则标注为"幻觉"（label=0）。只保留生成文本长度 ≥3 的非空答案
5. 使用 `sklearn.model_selection.train_test_split` 按标签**分层抽样**，确保三个集合的 hallucination 率一致

| 集合 | 用途 | 样本数 | Hallucination 率 |
|------|------|--------|-----------------|
| Set A（训练集） | 训练 336 个逐头分类器 | 500 | ~56.4% |
| Set B（验证集） | 评估分类器 AUC、筛选 Top-k 头 | 300 | ~56.7% |
| Set C（测试集） | 端到端幻觉检测评估 | 200 | ~56.5% |

---

## 步骤 2：影响向量提取与逐头分类器训练

### 什么是影响向量？

在 Transformer 中，每个注意力头 $h$ 在层 $l$ 向残差流贡献一个独立的增量向量：

$$\Delta_h = O_h \cdot W_O^h$$

其中 $O_h \in \mathbb{R}^{d_{head}}$ 为注意力头输出，$W_O^h \in \mathbb{R}^{d_{model} \times d_{head}}$ 为输出投影矩阵的对应切片（第 $h \cdot d_{head}$ 至 $(h+1) \cdot d_{head}$ 列），$\Delta_h \in \mathbb{R}^{d_{model}}$ 为该头对最终隐藏状态的直接影响。

### 提取流程

1. 对全部 28 层的 `o_proj` 注册 **forward hook**，捕获其输入（各头输出拼接后、投影前的向量），形状 `[1, T, 1536]`
2. 用保存的 `generated_ids` 直接拼接 prompt token IDs 进行前向传播（避免 decode→encode 往返误差）
3. 取**答案最后一个 token 位置**的激活值
4. 对每个头 $h$：切片 `attn_out[answer_last_pos, h*128:(h+1)*128]` → 与 `W_O[:, h*128:(h+1)*128]` 相乘 → 得到 1536 维影响向量
5. 预缓存所有 28 层的 $W_O$ 矩阵（减少 14000+ 次冗余 GPU→CPU 拷贝）

### 逐头分类器训练与头筛选

为全部 336 个注意力头各训练一个独立二分类器，在 Set A（500 条）上训练、Set B（300 条）上评估 AUC，按 AUC 降序选出 **Top-5** 头。

**Top-5 结果**：

| 排名 | 注意力头 | 所属层 | 验证集 AUC |
|------|----------|--------|-----------|
| 1 | **L16_H6** | 第 16 层 | 0.7313 |
| 2 | **L15_H6** | 第 15 层 | 0.7294 |
| 3 | **L11_H1** | 第 11 层 | 0.7189 |
| 4 | **L18_H0** | 第 18 层 | 0.7178 |
| 5 | **L13_H11** | 第 13 层 | 0.7166 |

最优头集中在第 11–18 层（中上层），与 SAPLMA 的最优 Layer 16 高度一致；AUC 热力图显示高性能头呈带状集中分布而非均匀散布。

---

## 步骤 3：探针重训练与多方法融合评估

### 探针重训练

对 Top-5 头在 Set A 上重新训练探针（而非直接复用步骤 2 的分类器），统一配置后在 Set C（200 条，**从未参与训练或头筛选**）上评估端到端性能。

### 六种评估方法

| 方法 | 特征 | 说明 |
|------|------|------|
| **PPL** | 标量（NLL） | 答案 token 平均负对数似然，取负值作为置信度 |
| **SAPLMA-L16** | 1536 维隐藏状态 | Layer 16 完整隐藏状态（`model.model.layers[15]`），SAPLMA 基线 |
| **Single-Head** | 1536 维影响向量 | 对 Top-5 头逐个训练独立探针（共 5 个） |
| **Summed-Heads** | 1536 维影响向量 | 5 个头的影响向量逐元素求和（$\sum_{i=1}^5 \Delta_{h_i}$），训练一个统一探针 |
| **Voting-Ensemble** | 5 个概率值 | 5 个单头探针各自输出概率，经各自阈值二值化后多数投票（≥3/5 判为幻觉），AUC 对概率取平均后计算 ROC |

---

## 阶段三 · 实验设置

### 模型

与阶段一/二相同，使用 **Qwen2-1.5B**（28 层，12 头/层，隐藏维度 1536，bfloat16 精度）。关键区别：阶段一/二直接使用隐藏状态作为探针输入；阶段三通过 forward hook 深入到**注意力头级别**捕获内部激活。

### 探针架构（全阶段统一）

```
输入 1536 维 → Dense(256, ReLU) → Dense(128, ReLU) → Dense(64, ReLU) → Dense(1, Sigmoid) → P(hallucination)
```

### 训练超参数（步骤 2 vs 步骤 3 对比）

| 超参数 | 步骤 2（头筛选） | 步骤 3（探针重训练） |
|--------|----------------|-------------------|
| 最大 Epoch | 50 | 5 |
| Early Stopping | patience=5 | 无 |
| Random Restarts | 1 | 3（取最佳） |
| Batch Size | 32 | 32 |
| 优化器/损失 | Adam / Binary Cross-Entropy | Adam / Binary Cross-Entropy |
| 输入预处理 | StandardScaler（fit A, transform B） | StandardScaler（fit A, transform C） |
| 阈值选取 | — | 训练集 ROC 最大化 Accuracy |

步骤 2 的 epoch 设高（50），依靠 early stopping 自动终止（大多数头在 10–30 轮停止）；步骤 3 用少量 epoch（5）+ 多次重启（3）以避免过拟合小训练集。

### 数据划分

| 集合 | 用途 | 样本数 | Hallucination 率 |
|------|------|--------|-----------------|
| Set A（训练集） | 训练 336 个逐头分类器 / 探针重训练 | 500 | ~56.4% |
| Set B（验证集） | 评估分类器 AUC、筛选 Top-k 头 | 300 | ~56.7% |
| Set C（测试集） | 端到端幻觉检测评估 | 200 | ~56.5% |

三个集合通过分层抽样保持 hallucination 率一致。

### SAPLMA 基线修复

SAPLMA 使用 1-based 层索引（Layer 16 = `hidden_states[16]` = `model.model.layers[15]` 的输出），原始代码错误地使用了 `model.model.layers[hidden_layer]`（0-indexed 偏移了一位），修复为 `model.model.layers[hidden_layer - 1]` 后确保基线对应正确的 Layer 16。

### 输出文件

- `head_aucs.csv`：全部 336 个头的验证集 AUC 排名
- `top5_heads.json`：Top-5 头的索引及 AUC
- `head_classifiers/`：Top-5 头的 Keras `.h5` 模型 + StandardScaler 参数
- `head_probe_metrics.csv`：所有方法的 Accuracy / Precision / Recall / F1 / AUC
- `fig_head_auc_heatmap.png`：28×12 AUC 热力图

---

## 步骤 3 结果：端到端幻觉检测（TriviaQA 测试集，200 条）

| 方法 | Accuracy | Precision | Recall | F1 | AUC |
|------|----------|-----------|--------|-----|-----|
| PPL | 0.615 | 0.593 | 0.368 | 0.454 | 0.603 |
| SAPLMA-L16 | 0.720 | 0.701 | 0.621 | 0.659 | 0.767 |
| Single-L16_H6 | 0.695 | 0.703 | 0.517 | 0.596 | 0.732 |
| Single-L15_H6 | 0.690 | 0.651 | 0.621 | 0.635 | 0.741 |
| Single-L11_H1 | 0.700 | 0.655 | 0.655 | 0.655 | 0.757 |
| Single-L18_H0 | 0.655 | 0.661 | 0.425 | 0.517 | 0.716 |
| Single-L13_H11 | 0.685 | 0.622 | 0.701 | 0.659 | 0.768 |
| Summed-Heads | 0.735 | 0.743 | 0.598 | 0.662 | 0.792 |
| **Voting-Ensemble** | **0.735** | **0.736** | 0.609 | **0.667** | **0.794** |

---

## 步骤 3 结果分析

| 对比维度 | 结论 |
|----------|------|
| **Summed-Heads vs SAPLMA** | 求和探针 AUC 0.792 > SAPLMA-L16 AUC 0.767，仅用 **5/336 个头**即超越完整 1536 维隐藏状态 |
| **特征级融合 vs 决策级融合** | Voting (0.794) 与 Summed-Heads (0.792) 基本持平，两种融合策略各有优势 |
| **PPL 的局限** | Recall 仅 0.368，大量幻觉样本漏检；AUC 0.603 勉强优于随机 |
| **单头 vs 多头** | 最优单头 L13_H11 AUC 0.768 已追平 SAPLMA 基线（0.767），验证了头级特征的判别力 |

---

## 实验结果总览

```
┌─────────────────────────────────────────────────────────────┐
│                    幻觉检测方法性能对比                        │
├──────────────┬──────────┬──────────┬──────────┬─────────────┤
│    方法       │  阶段    │   AUC    │ 特征维度  │   特点       │
├──────────────┼──────────┼──────────┼──────────┼─────────────┤
│ PPL          │  一/三   │  0.690   │    1     │ 无参数       │
│ SAPLMA-L16   │  一/三   │  0.843   │  1536    │ 完整隐藏状态  │
│ Voting-Ens.  │    三    │  0.794   │  5×1536  │ 投票决策      │
│ Summed-Heads │    三    │  0.792   │  1536    │ 5 个头影响向量│
│ Best Single  │    三    │  0.768   │  1536    │ 单个头特征    │
└──────────────┴──────────┴──────────┴──────────┴─────────────┘
```

注：SAPLMA AUC 0.843 为阶段一 six-topic true/false 数据集上的留一法平均；AUC 0.767 为阶段三 TriviaQA 测试集上的结果（任务难度不同，不可直接比较）。

---

# 关键发现与讨论

---

## 1. SAPLMA 为何远超 PPL？

| 因素 | PPL | SAPLMA |
|------|-----|--------|
| 信息量 | 1 个标量（NLL） | 1536 维隐藏状态 |
| 对表面特征的鲁棒性 | 受词频、句长影响大 | 编码抽象语义表征 |
| 判别能力 | 线性阈值分类 | 非线性前馈网络 |
| 内部信号 | 仅依赖输出概率 | 直接反映模型"内部认知" |

---

## 2. 倒 U 型层深度曲线

```
 AUC
  ↑
0.85│   ●
    │  / \         Layer 16 = 最优
0.80│ /   \
    │/     \
0.75│       ●
    │
0.70│
    └──┼──┼──┼──┼──┼──┼──┼──→ Layer
       14 16 18 20 22 24 26 28
```

加密测试间隔（每 2 层）后，峰值出现在 **Layer 16**（57% 深度），比此前 Layer 18 更靠前。中上层编码最抽象的任务相关表征；最后几层偏向于下一 token 预测任务，损失了部分真值判别信息。

---

## 3. 最后一个 Token 的关键作用

在自回归 Transformer 中，每个 token 的表示通过因果注意力机制聚合**所有前述 token** 的信息：

- Token 1：仅看到自己 → 几乎随机（AUC ≈ 0.50）
- Token -2：看到 98% 的句子 → 有一定判别力（AUC 0.690）
- Token -1：看到完整句子 → 信息最丰富（AUC 0.844）

**实验证实了"答案末尾 token 承载最多语义信息"的假设。**

---

## 4. 注意力头的可解释性

- 高性能头集中在第 11–18 层，与 SAPLMA 的最优层 Layer 16 位置吻合
- 仅 **5/336（约 1.5%）** 个头的影响向量即超越完整隐藏状态基线
- 说明真值判别信号在注意力头中是**高度集中**的，而非均匀分布
- 为事实知识定位（factual knowledge localization）和电路分析（circuit analysis）提供了新的实验证据

---

## 5. 特征级融合 vs 决策级融合

| 融合方式 | 方法 | AUC |
|----------|------|-----|
| 特征级融合 | Summed-Heads | 0.792 |
| 决策级融合 | Voting-Ensemble | 0.794 |

两种融合策略性能相当，说明 Top-5 头捕获了**互补的真值/幻觉判别信号**，无论在特征层面求和还是在决策层面投票均能达到相似的集成效果。

---

# 局限性与未来工作

1. **单一模型**：仅在 Qwen2-1.5B 上验证，对其他规模和架构的模型泛化性待确认
2. **数据集限制**：True/False 数据集使用受限格式，TriviaQA 使用模糊匹配自动标注引入噪声
3. **投票策略**：当前使用简单多数投票，更复杂的融合策略（加权投票、stacking）可能进一步提升
4. **计算开销**：逐头探针需前向传播提取特征，相比 PPL 增加了计算成本
5. **层与头的粒度**：仅探索了单层（L16）和单头级别，跨层多头组合可能有更好的效果

---

# 结论

1. **SAPLMA 显著优于 PPL**：隐藏状态比输出概率包含更丰富的真值判别信息（AUC 0.843 vs 0.690）
2. **最优检测层为 Layer 16**：加密测试（间隔 2 层）揭示峰值在 57% 深度处，最后 token 位置最关键（AUC 0.844）
3. **注意力头级探针可行且高效**：仅 5/336 个头，投票集成 AUC 达 0.794，超越完整 SAPLMA 基线（0.767）
4. **幻觉相关信息高度集中**：最优头集中在第 11–18 层，与 Layer 16 最优层位置吻合
5. **特征级与决策级融合相当**：求和探针（0.792）与投票集成（0.794）性能持平，两种策略均可行

**核心贡献**：证明可解释的注意力头级探针能以极少特征超越完整隐藏状态方法，同时揭示了对幻觉检测最关键的具体注意力头。

---

# 谢谢！欢迎提问
